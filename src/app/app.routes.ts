import { Routes } from '@angular/router';
import { PrayerTimesComponent } from './prayer-times/prayer-times.component';

export const routes: Routes = [
     { path: 'prayer-times-component', component: PrayerTimesComponent },
];
